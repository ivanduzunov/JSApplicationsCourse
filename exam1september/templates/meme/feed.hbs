{{> navbar}}
<div id="meme-feed">
    <h1>Meme Feed</h1>
    <div id="memes">
        {{#if noMemes}}
            <p class="no-memes">No memes in database.</p>
        {{else}}
            {{#each memes}}
                {{> memeListing}}
            {{/each}}
        {{/if}}

    </div>
</div>
{{> footer}}
