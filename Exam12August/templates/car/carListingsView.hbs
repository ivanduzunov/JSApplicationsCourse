{{> navbar}}
<div id="car-listings">
    <h1>Car Listings</h1>
    <div id="listings">
        {{#if noCars}}
            <p class="no-cars">No cars in database.</p>
        {{else}}
            {{#each cars}}
                {{> carListing}}
            {{/each}}
        {{/if}}
    </div>
</div>
{{> footer}}