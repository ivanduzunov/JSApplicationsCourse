{{> navbar}}
<div class="my-listings">
    <h1>My car listings</h1>
    <div class="car-listings">
        {{#if noCars}}
            <p class="no-cars">No your cars in database.</p>
        {{else}}
            {{#each cars}}
                {{> myListing}}
            {{/each}}
        {{/if}}
    </div>
</div>
{{> footer}}